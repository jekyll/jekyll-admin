language: ruby
rvm: 2.2
before_install:
  - apt-get install build-essential
  - nvm install 7
  - npm install -g node-gyp
install: script/bootstrap
script: script/cibuild
cache:
  bundler: true
  yarn: true
sudo: false
env:
  matrix:
    - TEST_SUITE=node
    - TEST_SUITE=ruby
