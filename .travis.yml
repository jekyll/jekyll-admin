language: ruby
rvm: 2.2
before_install:
  - nvm install 7
install: script/bootstrap
script: script/cibuild
cache:
  bundler: true
  yarn: true
sudo: false
env:
  matrix:
    - TEST_SUITE=node
    - TEST_SUITE=ruby
